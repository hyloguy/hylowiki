<!-- 
hylowiki
GA WDI Project 1
Author: Michael N. Rubinstein
 -->

<!doctype html>
<html lang="en">
<head>
	<title>HyloWiki</title>
    <link 
        rel="stylesheet"
        type="text/css"
        href="/public/semantic_ui/css/semantic.min.css">
</head>
<body>
    <h1 class="ui purple header">
        hylowiki
        <div class="sub header">The Wiki of the Material Universe.</div>
    </h1>
    <h4 class="ui black block header">
        <%= page.title %>
    </h4>
    <div class="ui primary segment">
    	<p><%= markdown.render(page.body) %></p>
    </div>
    <div class="ui secondary segment">
    	<p>Edit by
    		<%= user.username %>
    		at 
    		<%= Time.at(page.time_stamp).strftime("%T%p on %m/%d/%Y") %>.</p>
    </div>
    <div class="ui secondary segment">
    	<p>Page History:</p>
	    <div class="ui list">
	    	<% history.each do |page| %>
	    		<a class="item" href="/pages/show?id=<%=page.id%>">
	    			<%= Time.at(page.time_stamp).strftime("%T%p on %m/%d/%Y ") %>
	    			by <%= page.author.username %>
	    		</a>
	    	<% end %>
	    </div>
    </div>
</body>
</html>
